build/
build_host/
sdkconfig.old
managed_components/
dependencies.lock
.vscode/
*.bin
*.elf
*.map
__pycache__/
.DS_Store
build-xiao/
build_xiao/
build_release/
build_gate/
build-bench/
build_bench/
# Any future out-of-tree build directory. The named entries above stay so the
# common ones are obvious, but a new -B name must not be able to add 100k
# lines of CMake scratch to a commit, which is exactly what build-bench did.
build-*/
build_*/
sdkconfig
compile_commands.json
